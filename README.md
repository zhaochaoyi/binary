# To Diversify or Not to Diversify?

Static GitHub Pages site for the updated Lo, Weinstock & Zhao binary-asset paper.

## Local preview

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.

## Publish

Push this folder as the root of a GitHub Pages repository, for example a repository named `binary` served from the `main` branch.
